# Codex agent image

This directory is reserved for the separately built Codex vendor agent image.
It is not the `thinkpixel-agentd` supervisor image and remains deferred to
Phase 5, when Codex App Server and its runtime dependencies can be pinned and
qualified together.
